\tikzset{msFlipFlop/.style = {muxdemux, muxdemux def = {
    NL = 2, NR = 2, NB = 2, NT = 6,
    Lh = 3, Rh = 3,
    w = 5
    },
    draw only left pins = {2},
    draw only top pins = {1,2},
    draw only bottom pins = {2},
    }
}

\tikzset{edgeDetector/.style = {muxdemux, muxdemux def = {
    NL = 1, NR = 1, NB = 0, NT = 0,
    Lh = 1, Rh = 1,
    w = 2
    },
    }
}

% \ctikzset{tripoles/american and port/width=.055,tripoles/american and port/height=.04}
