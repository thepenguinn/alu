\tikzset{mux02/.style = {muxdemux, muxdemux def = {
    NL = 2, NR = 1, NB = 1, NT = 0,
    % Lh = 13, Rh = 7.8,
    Lh = 3, Rh = 1.5,
    % inset Lh = 4.5, inset Rh = 2.75,
    % inset Lh = 3.5, inset Rh = 1.75,
    % inset w = 1,
    % w = 4.5,
    w = 1.25,
    square pins = 1
    },
    % draw only left pins={2-5,6-9},
    % draw only right pins={1-2},
    % draw only bottom pins={2-4}
    }
}
